create or replace TABLE INCONTACT_DL_ALL_CALLBACKS (
	ADD_DATE VARCHAR(100),
	FIRST_NAME VARCHAR(500),
	LAST_NAME VARCHAR(500),
	NUMBER_TO_DIAL NUMBER(38,0),
	SCHEDULED_DOW VARCHAR(500),
	SCHEDULED_START VARCHAR(500),
	SCHEDULED_END VARCHAR(500),
	SKILL NUMBER(38,0),
	CALLEDBACK VARCHAR(500)
);