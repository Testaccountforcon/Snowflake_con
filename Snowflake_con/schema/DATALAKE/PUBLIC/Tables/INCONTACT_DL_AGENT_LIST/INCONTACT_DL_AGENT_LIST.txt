create or replace TABLE INCONTACT_DL_AGENT_LIST (
	AGENT_NO NUMBER(11,0),
	TEAM_NO NUMBER(11,0),
	FIRST_NAME VARCHAR(200),
	LAST_NAME VARCHAR(200),
	EMAIL VARCHAR(200),
	MOD_DATETIME VARCHAR(500),
	LAST_LOGIN VARCHAR(500),
	STATUS VARCHAR(50)
);